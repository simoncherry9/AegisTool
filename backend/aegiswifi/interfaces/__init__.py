"""Módulo de interfaces inalámbricas (Fase 3, minuta §13, §37).

Detección, preparación y restauración de interfaces Wi-Fi:
  - Detección (listar interfaces, chipset, driver, PHY, MAC).
  - Capacidades (modos, bandas, canales, monitor mode, inyección).
  - Monitor mode (activar/desactivar, interfaz virtual).
  - Restauración (guardar/restaurar estado original).
  - Diagnóstico (rfkill, procesos conflictivos, health check).
"""
